# Tag Management Operations

# ============================================
# QUERIES
# ============================================

query GetTags($query: String, $limit: Int) {
  tags(query: $query, limit: $limit) {
    id
    slug
    label
    createdAt
    updatedAt
  }
}

query GetTag($id: ID, $slug: String) {
  tag(id: $id, slug: $slug) {
    id
    slug
    label
    createdAt
    updatedAt
  }
}

query CheckTagSlugAvailable($slug: String!) {
  checkTagSlugAvailable(slug: $slug)
}

query GetTagUsageCount($slug: String!) {
  getTagUsageCount(slug: $slug)
}

# ============================================
# MUTATIONS
# ============================================

mutation CreateTag($input: CreateTagInput!) {
  createTag(input: $input) {
    id
    slug
    label
    createdAt
    updatedAt
  }
}

mutation UpdateTag($id: ID!, $input: UpdateTagInput!) {
  updateTag(id: $id, input: $input) {
    id
    slug
    label
    createdAt
    updatedAt
  }
}

mutation DeleteTag($id: ID!) {
  deleteTag(id: $id)
}
