# =============================================================================
# User Preference Fragments
# =============================================================================
# Fragments for user notification preferences and mutes.

fragment NotificationPreferenceCore on NotificationPreference {
  id
  userId
  emailOnInvite
  emailOnJoinRequest
  emailOnMessage
  pushOnReminder
  inAppOnEverything
  createdAt
  updatedAt
}

fragment EventMuteCore on EventMute {
  id
  eventId
  userId
  muted
  createdAt
}
