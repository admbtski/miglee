# =============================================================================
# Event Invite Link Fragments
# =============================================================================
# Fragments for event invitation links.

fragment EventInviteLinkCore on EventInviteLink {
  id
  eventId
  code
  maxUses
  usedCount
  expiresAt
  createdAt
  isExpired
  isMaxedOut
  isValid
}
