# =============================================================================
# Worker ServiceAccount - minimal permissions for all workers
# =============================================================================
apiVersion: v1
kind: ServiceAccount
metadata:
  name: worker
  labels:
    app.kubernetes.io/name: appname
    app.kubernetes.io/component: worker
automountServiceAccountToken: false  # Workers don't need K8s API access

