# =============================================================================
# Web ServiceAccount - minimal permissions
# =============================================================================
apiVersion: v1
kind: ServiceAccount
metadata:
  name: web
  labels:
    app.kubernetes.io/name: appname
    app.kubernetes.io/component: web
automountServiceAccountToken: false  # Web doesn't need K8s API access

